version: 2

models:
  - name: dim_credit_card
    columns:
      - name: sk_credit_card
        description: "SK of dim_credit_card"
        tests:
          - unique
          - not_null   

      - name: id_credit_card
        description: "Card ID"

      - name: card_type
        description: "Card type"