version: 2

models:
  - name: dim_region
    columns:
      - name: sk_ship_to_address
        description: "SK for the region table."
        tests:
          - unique
          - not_null

      - name: fk_ship_to_address
        description: "FK for the region table."
        tests:          
          - not_null
             
      - name: city
        description: "City name."

      - name: state
        description: "State name."
        
      - name: country
        description: "Country name."